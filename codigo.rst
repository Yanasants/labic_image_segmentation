LabIC Images Segmentation
**************************


**Code Blocks**: Class.

.. code-block:: python

    def save_dice(adress):
        dice = 'teste'
        with open(adress, "w") as file:
            file.write(f"Dice: {dice}")

**Codigo via autodoc**: testando autodoc.

.. automodule:: labic_images_segmentation
    :members:

    